{
    "builds": [{
        "src": "Insight/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "your_project_name/wsgi.py"
        }
    ]
}