{
    "builds": [{
        "src": "Insight/wsgi.py",
        "use": "@vercel/python",
        "config": { 
            "maxLambdaSize": "15mb",
            "installCommand": "pip install --no-cache-dir -r requirements.txt"
        }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "Insight/wsgi.py"
        }
    ]
}